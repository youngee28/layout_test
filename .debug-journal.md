# Debug Journal - git_bash MCP startup warning
Started: 2026-06-26T17:30:01+09:00
Goal: Resolve Codex startup warning for `git_bash` MCP handshake closing during initialize.

## Environment snapshot (Phase 0)
- Runtime: Node MCP server launched from OMO plugin `components/git-bash-mcp/dist/cli.js`.
- Entry: `node ./components/git-bash-mcp/dist/cli.js mcp` from `/home/youngee/.codex/plugins/cache/sisyphuslabs/omo/4.13.0`.
- Ports / sockets: stdio MCP only.
- Git HEAD: `e970f4faf7fff9bcddea710e4c8d2924df3719a0`; working tree had pre-existing modified app files.
- References read: `debugging/SKILL.md`, `references/methodology/00-setup.md`, `references/methodology/02-investigate.md`, `references/runtimes/node.md`.

## Hypotheses
1. [CONFIRMED] `git_bash` is enabled on a non-Windows host, and the MCP server exits before stdio handshake because it is Windows-only. Distinguishing evidence: server `runMcpStdioServer` returns when `canRunGitBash(options)` is false; `process.platform` is non-win32. If true, fix is: disable server.
2. [REFUTED] PATH `node` points to Bun and cannot execute the MCP server. Distinguishing evidence: server script launched with PATH `node` and exited 0 on EOF; no stack trace. If true, fix is: pin node.
3. [REFUTED] The configured MCP entry path or cwd is wrong. Distinguishing evidence: `.mcp.json` path exists and direct execution reaches CLI. If true, fix is: restamp path.

## Failed hypothesis round counter
- Round 1: H1 confirmed, H2/H3 refuted.

## Artifacts to revert
- [ ] `.debug-journal.md` - local debug journal. Revert: remove file.
- [ ] `/home/youngee/.codex/config.toml` - add Linux-safe plugin MCP policy disabling `git_bash`. Revert: remove the `plugins."omo@sisyphuslabs".mcp_servers.git_bash` block if needed.

## Findings
### 2026-06-26T17:28:15+09:00 - ULW CLI path
- Source: `omo ulw-loop create-goals --brief ... --json`
- Value: `zsh:1: command not found: omo`
- Interpretation: ULW state CLI is not on PATH in this environment.
- Refutes/Confirms: Process fallback needed.

### 2026-06-26T17:29:00+09:00 - MCP manifest
- Source: `/home/youngee/.codex/plugins/cache/sisyphuslabs/omo/4.13.0/.mcp.json`
- Value: `git_bash` has `"command": "node"`, `"args": ["./components/git-bash-mcp/dist/cli.js", "mcp"]`, `"cwd": "."`.
- Interpretation: Codex starts the OMO git-bash MCP unconditionally unless per-server policy disables it.
- Refutes/Confirms: Refutes H3 path-missing.

### 2026-06-26T17:29:30+09:00 - Runtime behavior
- Source: `components/git-bash-mcp/dist/cli.js`
- Value: `runMcpStdioServer` returns immediately when `!canRunGitBash(options)`; `canRunGitBash` requires `platformFromOptions(options) === "win32"`.
- Interpretation: On Linux the process exits before responding to `initialize`, causing client handshake closed.
- Refutes/Confirms: Confirms H1.

### 2026-06-26T17:29:50+09:00 - Installer intended config
- Source: `components/bootstrap/dist/cli.js`
- Value: `const gitBashEnabled = (input.platform ?? process.platform) === "win32" && input.gitBashEnabled === true;` then `ensurePluginMcpEnabled(..., "git_bash", gitBashEnabled)`.
- Interpretation: Linux installs should have `git_bash` per-server enabled false in Codex config.
- Refutes/Confirms: Confirms minimal config fix.

## Final fix
- Pending.
